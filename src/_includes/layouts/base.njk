<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1">

  {# Global stylesheets shared by the entire site #}
  <link rel="stylesheet" href="https://datachefs.org/assets/css/style.css">
  <link rel="stylesheet" href="https://datachefs.org/assets/css/matoolkit.css">

  <title>{{ title }}</title>

  {# Optional: Allows individual layouts or pages to inject extra <head> content (like custom stylesheets) #}
  {% block extrahead %}{% endblock %}
</head>
<body>
  <header class="site-header">
    <div class="wrapper">
      {# Navigation bar — loaded from a partial so it can be reused across pages #}
      {% include "partials/nav.njk" %}
    </div>
  </header>

  <main class="page-content" aria-label="Content">
    <div class="wrapper">
      {# Main content area — layouts like 'example.njk' can override this block #}
      {% block content %}
        {{ content | safe }}
      {% endblock %}
    </div>
  </main>

  <footer class="site-footer h-card">
    <data class="u-url" href="/"></data>
    <div class="wrapper">
      <div class="footer-col-wrapper">
        &copy; 2025 Datachefs.org
      </div>
    </div>
  </footer>

  {# Optional: Allows layouts/pages to include their own <script> tags at the bottom of the page #}
  {% block extrascript %}{% endblock %}
</body>
</html>
